import { ProdutFormDirective } from './produt-form.directive';

describe('ProdutFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ProdutFormDirective();
    expect(directive).toBeTruthy();
  });
});
